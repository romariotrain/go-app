# Веб-приложение "Блог" на Go

Простое веб-приложение с бэкендом на Go и фронтендом на HTML/CSS/JavaScript.

## Архитектура проекта

Проект состоит из двух основных частей:

### Бэкенд (Go)
- REST API сервер на порту 8080
- Хранение данных в памяти (без базы данных для простоты)
- CRUD операции для постов блога
- Поддержка CORS для взаимодействия с фронтендом

### Фронтенд (HTML/CSS/JS)
- Современный интерфейс с градиентным дизайном
- Динамическое взаимодействие с API
- Создание, чтение, обновление и удаление постов
- Адаптивный дизайн для мобильных устройств

## Структура проекта

```
webapp/
├── backend/
│   ├── main.go       # Основной файл Go-сервера
│   └── go.mod        # Файл зависимостей Go
└── frontend/
    ├── index.html    # Главная страница
    ├── styles.css    # Стили оформления
    └── app.js        # Логика фронтенда
```

## API Endpoints

Бэкенд предоставляет следующие эндпоинты:

- `GET /` - Health check (проверка работы сервера)
- `GET /api/posts` - Получить все посты
- `POST /api/posts` - Создать новый пост
- `GET /api/posts/:id` - Получить пост по ID
- `PUT /api/posts/:id` - Обновить пост
- `DELETE /api/posts/:id` - Удалить пост

## Как запустить

### 1. Запуск бэкенда

Откройте терминал и перейдите в директорию бэкенда:

```bash
cd /home/claude/webapp/backend
```

Запустите Go-сервер:

```bash
go run main.go
```

Вы должны увидеть сообщение:
```
🚀 Сервер запущен на http://localhost:8080
```

Бэкенд теперь работает и готов принимать запросы!

### 2. Запуск фронтенда

Есть несколько способов запустить фронтенд:

#### Способ 1: Простой HTTP-сервер на Python

Откройте новый терминал и перейдите в директорию фронтенда:

```bash
cd /home/claude/webapp/frontend
```

Запустите HTTP-сервер:

```bash
python3 -m http.server 3000
```

Теперь откройте браузер и перейдите по адресу: `http://localhost:3000`

#### Способ 2: HTTP-сервер на Node.js

Если у вас установлен Node.js, можно использовать http-server:

```bash
npx http-server -p 3000
```

#### Способ 3: Открыть файл напрямую

Можно просто открыть файл `index.html` в браузере, но при этом могут возникнуть проблемы с CORS. Лучше использовать один из HTTP-серверов выше.

## Проверка работы

1. После запуска бэкенда и фронтенда, откройте браузер
2. Перейдите на `http://localhost:3000`
3. Вы должны увидеть страницу блога с двумя тестовыми постами
4. Попробуйте создать новый пост, используя форму
5. Попробуйте отредактировать или удалить пост

## Тестирование API с помощью curl

Вы можете протестировать API напрямую, используя curl:

### Получить все посты
```bash
curl http://localhost:8080/api/posts
```

### Создать новый пост
```bash
curl -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{"title":"Тестовый пост","author":"Test User","content":"Это тестовое содержимое"}'
```

### Получить конкретный пост
```bash
curl http://localhost:8080/api/posts/1
```

### Обновить пост
```bash
curl -X PUT http://localhost:8080/api/posts/1 \
  -H "Content-Type: application/json" \
  -d '{"title":"Обновленный заголовок","author":"Admin","content":"Обновленное содержимое"}'
```

### Удалить пост
```bash
curl -X DELETE http://localhost:8080/api/posts/1
```

## Возможные улучшения

Это базовая версия приложения. Вот несколько идей для дальнейшего развития:

- Добавить постоянное хранилище данных (PostgreSQL, MySQL, SQLite)
- Добавить аутентификацию и авторизацию пользователей
- Добавить пагинацию для списка постов
- Добавить поиск по постам
- Добавить категории и теги для постов
- Добавить комментарии к постам
- Добавить загрузку изображений
- Добавить Markdown-форматирование для постов
- Добавить Docker для упрощения развертывания
- Добавить тесты для бэкенда и фронтенда

## Технологии

### Бэкенд
- Go 1.21+
- Только стандартная библиотека (net/http, encoding/json)

### Фронтенд
- HTML5
- CSS3 (с градиентами и анимациями)
- Vanilla JavaScript (ES6+)
- Fetch API для HTTP-запросов

## Лицензия

Этот проект создан в учебных целях и может свободно использоваться и модифицироваться.